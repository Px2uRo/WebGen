//是的，我抄了 Avalonia (Preresx 那个时候) 的代码，我也是初学者，我想要也需要一个简单的起点。没事，我也是 MIT 协议
// ↑ 以上内容是 Visual Studio 的 共同飞行员 写的注释。

using System;
using System.Collections.Generic;
using System.Text;

namespace WebGen.BasicControls
{
    /// <summary>
    /// 迟早会被替换成App.g.cs的。
    /// 封装 Avalonia 应用程序的类。
    /// 是的我抄了 Avalonia 的代码，我也是初学者，我需要一个简单的起点。没事，我也是 MIT 协议
    /// </summary>
    /// <summary>
    /// </summary>
    /// <remarks>
    /// Application 类封装了 Avalonia 应用程序相关的功能，包括：
    /// - 一组全局数据模板（DataTemplates）。
    /// - 一组全局样式集合（Styles）。
    /// - 应用的焦点管理器（FocusManager）。
    /// - 应用的输入管理器（InputManager）。
    /// - 在 RegisterServices 方法中注册 Avalonia 其他部分所需的服务。
    /// - 管理应用程序生命周期。
    /// </remarks>
    public class Application
    {
        
    }
}
